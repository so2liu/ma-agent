name: Nightly Release

on:
  schedule:
    - cron: '0 0 * * *' # UTC 00:00 daily
  workflow_dispatch: # allow manual trigger

concurrency:
  group: nightly-release
  cancel-in-progress: true

jobs:
  check-changes:
    runs-on: ubuntu-latest
    outputs:
      has_changes: ${{ steps.check.outputs.has_changes }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if there are new commits since last nightly
        id: check
        run: |
          # For manual trigger, always build
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Check if any commits in last 24 hours
          LAST_24H=$(git log --since="24 hours ago" --oneline | wc -l)
          if [ "$LAST_24H" -gt 0 ]; then
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          fi

  build-macos:
    needs: check-changes
    if: needs.check-changes.outputs.has_changes == 'true'
    runs-on: macos-latest
    strategy:
      matrix:
        arch: [arm64]
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Generate nightly version
        id: nightly-version
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          DATE=$(date -u +%Y%m%d)
          BASE_VERSION=$(node -p "require('./package.json').version")
          # Use a higher patch version so nightly > stable in semver
          IFS='.' read -r MAJOR MINOR PATCH <<< "$BASE_VERSION"
          NEXT_PATCH=$((PATCH + 1))
          NIGHTLY_VERSION="${MAJOR}.${MINOR}.${NEXT_PATCH}-nightly.${DATE}.${SHORT_SHA}"
          echo "nightly_version=${NIGHTLY_VERSION}" >> "$GITHUB_OUTPUT"
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Set nightly version in package.json
        run: node -e "const fs = require('fs'); const p = JSON.parse(fs.readFileSync('package.json','utf-8')); p.version = '${{ steps.nightly-version.outputs.nightly_version }}'; fs.writeFileSync('package.json', JSON.stringify(p, null, 2) + '\n');"

      - name: Install dependencies
        run: bun install --no-save

      - name: Lint & typecheck
        run: bun run lint && bun run typecheck

      - name: Test
        run: bun run test

      - name: Build Electron app
        run: bun run build

      - name: Package for macOS (${{ matrix.arch }})
        env:
          # Apple code signing
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          # Apple notarization
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          npx electron-builder --mac --${{ matrix.arch }} --publish never

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-${{ matrix.arch }}
          path: |
            dist/*.zip
            dist/*.dmg
            dist/*.blockmap
            dist/latest-mac*.yml
          retention-days: 7

  publish-nightly:
    needs: build-macos
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Get nightly version info
        id: version
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          NIGHTLY_VERSION=$(node -p "require('./package.json').version")
          echo "nightly_version=${NIGHTLY_VERSION}" >> "$GITHUB_OUTPUT"
          echo "short_sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Create nightly prerelease (replacing previous)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Create new release first, then delete old one to minimize downtime
          TEMP_TAG="nightly-$(date -u +%s)"
          gh release create "$TEMP_TAG" \
            --title "Nightly Build (${{ steps.version.outputs.short_sha }})" \
            --notes "Automated nightly build from \`main\` branch.

          **Commit:** ${{ github.sha }}
          **Version:** ${{ steps.version.outputs.nightly_version }}
          **Date:** $(date -u +%Y-%m-%dT%H:%M:%SZ)

          > This is a pre-release build and may be unstable. Enable the nightly update channel in Settings to receive these updates automatically." \
            --prerelease \
            dist/*

          # Delete old nightly release if it exists
          gh release delete nightly --yes --cleanup-tag 2>/dev/null || true

          # Rename the new release tag to 'nightly'
          gh release edit "$TEMP_TAG" --tag nightly
          git tag -d "$TEMP_TAG" 2>/dev/null || true
          gh api -X DELETE "repos/${{ github.repository }}/git/refs/tags/$TEMP_TAG" 2>/dev/null || true
